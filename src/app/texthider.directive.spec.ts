import { TexthiderDirective } from './texthider.directive';

describe('TexthiderDirective', () => {
  it('should create an instance', () => {
    const directive = new TexthiderDirective();
    expect(directive).toBeTruthy();
  });
});
